notifications:
  email: false

sudo: required

services:
  - docker

git:
  depth: false

script:
  - docker build . -t renku/renku-demo

deploy:
  - provider: script
    skip_cleanup: true
    script: bash travis-deploy.sh
    on:
      branch: master
